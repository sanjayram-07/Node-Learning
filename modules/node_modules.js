const { log } = require('console');
const os = require('os');
const fs = require('fs');

fs.writeFileSync("hello.txt","hi this is sanjay ram ");
const data = fs.readFileSync("hello.txt","utf8");
console.log(data);

console.log(os.uptime());
console.log(os.cpus());
console.log(os.freemem());
console.log(os.platform());
